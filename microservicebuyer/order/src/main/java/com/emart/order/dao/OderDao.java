package com.emart.order.dao;

import com.emart.order.entity.Item;

import org.springframework.stereotype.Repository;

@Repository
public interface OderDao{

    
    // @Query(value = "SELECT u FROM Item u WHERE category_id=:category_id")
    // public Item queryItemById(@Param("category_id") long category_id);


    
}